services:
  grader:
    build: .
    ports:
      - "8080:8080"
    read_only: true
    tmpfs:
      - /tmp
      - /work:exec,size=512m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    restart: unless-stopped
